public void ProcessData()
{
    var data = File.ReadAllLines("data.txt");
    foreach (var line in data)
    {
        // Complex logic here...
    }
    File.WriteAllText("output.txt", "Processed data");
}